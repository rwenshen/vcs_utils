[project]
name = "vcs-utils"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Shen HuiLiang", email = "shenhuiliang@virtuos.com.cn" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
vcs-utils = "vcs_utils:main"

[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "uv_build"
